name: EC2 Control

on:
  workflow_dispatch:
    inputs:
      action:
        description: 'Action (start or stop)'
        required: true
        default: 'start'

jobs:
  control-ec2:
    runs-on: ubuntu-latest
    steps:
      - name: Start or Stop EC2 instance
        run: |
            export AWS_REGION=ap-south-1
            export AWS_DEFAULT_REGION=ap-south-1
            export AWS_ACCESS_KEY_ID=AKIA2DP4K3YWIGSWFSVF
            export AWS_SECRET_ACCESS_KEY=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
            aws ec2 start-instances --region ap-south-1 --instance-ids i-04be160031028ac2e
